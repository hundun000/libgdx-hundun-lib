eclipse.project.name = appName + '-gamelib'

publishing {
    publications {
        mavenJava(MavenPublication) {
            // useless for jitpack: groupId = GROUP
            artifactId = ARTIFACT_ID_START + "-gamelib"
            // useless for jitpack: version = VERSION

            from components.java
        }
    }
}
